# Build LLVM-enabled bootstrap sig compiler binaries using cmake + ninja.
# Requires: pre-built LLVM 22 from build-llvm.yaml (release tag: llvm-22)
#
# All platforms build natively on their own runner using cmake + zig as CC.
# Chain: build-llvm (one-time) → build-bootstrap (one-time) → release (every release)
name: build-bootstrap

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Bootstrap release tag'
        required: false
        default: 'bootstrap-sig-v30'

permissions:
  contents: write

env:
  VERSION: ${{ github.event.inputs.version || 'bootstrap-sig-v30' }}
  ZIG_VERSION: "0.14.1"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-linux
            llvm_asset: llvm-22-x86_64-linux-native.tar.xz
            artifact: bootstrap-sig-x86_64-linux
            bin: sig
            deps: sudo apt-get update -qq && sudo apt-get install -y -qq cmake ninja-build zlib1g-dev libzstd-dev
            zlib_flag: "-DZLIB=$(find /usr/lib/x86_64-linux-gnu -name 'libz.a' 2>/dev/null | head -1)"
            zstd_flag: "-DZSTD=$(find /usr/lib/x86_64-linux-gnu -name 'libzstd.a' 2>/dev/null | head -1)"
          - os: windows-latest
            target: x86_64-windows
            llvm_asset: llvm-22-x86_64-windows.tar.xz
            artifact: bootstrap-sig-x86_64-windows
            bin: sig.exe
            deps: echo "deps ready"
            zlib_flag: "-DZIG_NO_ZLIB=ON"
            zstd_flag: "-DZIG_NO_ZSTD=ON"
          - os: macos-14
            target: aarch64-macos
            llvm_asset: llvm-22-aarch64-macos.tar.xz
            artifact: bootstrap-sig-aarch64-macos
            bin: sig
            deps: brew install cmake ninja zlib zstd
            zlib_flag: "-DZLIB=$(find /opt/homebrew/lib -name 'libz.a' 2>/dev/null | head -1)"
            zstd_flag: "-DZSTD=$(find /opt/homebrew/lib -name 'libzstd.a' 2>/dev/null | head -1)"
    runs-on: ${{ matrix.os }}
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install build deps
        shell: bash
        run: ${{ matrix.deps }}

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: ${{ env.ZIG_VERSION }}

      - name: Download pre-built LLVM 22
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release download llvm-22 --pattern "${{ matrix.llvm_asset }}" --dir /tmp
          mkdir -p "$PWD/llvm-22"
          tar -xJf "/tmp/${{ matrix.llvm_asset }}" -C "$PWD/llvm-22" --strip-components=1
          rm "/tmp/${{ matrix.llvm_asset }}"
          echo "LLVM_PREFIX=$PWD/llvm-22" >> "$GITHUB_ENV"

      - name: Build with cmake + ninja
        shell: bash
        run: |
          export PATH="$PWD/llvm-22/bin:$PATH"
          mkdir build && cd build
          cmake .. \
            -DCMAKE_INSTALL_PREFIX="$PWD/stage3" \
            -DCMAKE_PREFIX_PATH="${LLVM_PREFIX}" \
            -DCMAKE_BUILD_TYPE=Release \
            -DZIG_USE_LLVM_CONFIG=ON \
            -DZIG_NO_LIB=ON \
            -DZIG_TARGET_MCPU="baseline" \
            ${{ matrix.zlib_flag }} \
            ${{ matrix.zstd_flag }} \
            -GNinja
          ninja zig2 -j$(nproc 2>/dev/null || sysctl -n hw.ncpu 2>/dev/null || echo 4)
          file zig2 || file zig2.exe || true

      - name: Package and publish
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p ${{ matrix.artifact }}/bin
          if [ -f build/zig2.exe ]; then
            cp build/zig2.exe ${{ matrix.artifact }}/bin/sig.exe
            cp build/zig2.exe ${{ matrix.artifact }}/bin/zig.exe
          else
            cp build/zig2 ${{ matrix.artifact }}/bin/sig
            cp build/zig2 ${{ matrix.artifact }}/bin/zig
          fi
          tar -czf ${{ matrix.artifact }}.tar.gz -C ${{ matrix.artifact }} .
          gh release view "${VERSION}" >/dev/null 2>&1 || \
            gh release create "${VERSION}" \
              --title "Bootstrap Sig Compiler ${VERSION}" \
              --notes "LLVM-enabled bootstrap sig built with cmake+ninja+zig. All platforms native." \
              --prerelease
          gh release upload "${VERSION}" ${{ matrix.artifact }}.tar.gz --clobber
